import { GenericVideoComponentProps } from '@interfaces/ComponentProps';

export function ImageMagick({
  videoRef,
  detectorRef,
  handleMagickSwitch,
}: GenericVideoComponentProps) {
  return <div>ImageMagick menu!</div>;
}
